{
  "name": "@plataforma-ofertas/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "^4.28.1",
    "@plataforma-ofertas/database": "*",
    "@plataforma-ofertas/domain": "*",
    "@plataforma-ofertas/shared": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "ts-node-dev": "^2.0.0",
    "vitest": "^2.0.5"
  }
}
